@model MvcRecordStore.Models.ViewModels.AdminIndexVM

@{
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
    ViewData["Title"] = "Dashboard";
}

<div class="container mt-4">
    <div class="row">
        <div class="col-md-3">
            <div class="card text-white bg-primary mb-3">
                <div class="card-body text-center">
                    <h5 class="card-title">Total Artists</h5>
                    <h2 class="card-text">@Model.TotalArtists</h2>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-success mb-3">
                <div class="card-body text-center">
                    <h5 class="card-title">Total Genres</h5>
                    <h2 class="card-text">@Model.TotalGenres</h2>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-warning mb-3">
                <div class="card-body text-center">
                    <h5 class="card-title">Total Labels</h5>
                    <h2 class="card-text">@Model.TotalLabels</h2>
                </div>
            </div>
        </div>
        <div class="col-md-3">
            <div class="card text-white bg-danger mb-3">
                <div class="card-body text-center">
                    <h5 class="card-title">Total Records</h5>
                    <h2 class="card-text">@Model.TotalRecords</h2>
                </div>
            </div>
        </div>
        
        <!-- fill this with something
        <div class="row mt-4">
            <div class="col-md-8 mx-auto">
                <div class="card border rounded-0 border-2 shadow-sm">
                    <div class="card-header bg-dark text-white">
                        <h5>Quick Post Creation</h5>
                    </div>
                    <div class="card-body">
                        <form asp-controller="Posts" asp-action="Create">
                            <div class="mb-3">
                                <label asp-for="Post.Title" for="title" class="form-label">Title</label>
                                <input type="text" asp-for="Post.Title" class="form-control" id="title"
                                    placeholder="Enter post title">
                                <span asp-validation-for="Post.Title" class="text-danger"></span>
                            </div>
                            <div class="mb-3">
                                <label asp-for="Post.Body" for="froala-editor" class="form-label">Body</label>
                                <textarea asp-for="Post.Body" class="form-control" id="froala-editor" rows="4"
                                    placeholder="Enter post content"></textarea>
                                <span asp-validation-for="Post.Title" class="text-danger"></span>
                            </div>
                            <div class="mb-3">
                                <label asp-for="Post.SelectedCategories" for="categories"
                                    class="form-label">Categories</label>
                                <select asp-for="Post.SelectedCategories" class="form-select" id="categories" multiple
                                    aria-label="Multiple select example">
                                    @@foreach (var item in ViewBag.Categories)
                                    {
                                        <option value="@@item.ID">@@item.Title</option>
                                    }
                                </select>
                                <span asp-validation-for="Post.SelectedCategories" class="text-danger"></span>
                            </div>
                            <button type="submit" class="btn btn-primary">Create Post</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>  -->
    </div>
</div>